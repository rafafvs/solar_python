% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sGARCH-internals.R
\name{sGARCH_params_to_phi}
\alias{sGARCH_params_to_phi}
\title{Map unconstrained parameters to constrained GARCH coefficients}
\usage{
sGARCH_params_to_phi(zeta, p, q, mode = "unitOmega", eps = 0)
}
\arguments{
\item{zeta}{Numeric vector of unconstrained parameters: \code{eta0} (mass),
\code{kappa_1..kappa_{p+q}} (softmax logits), and optionally \code{xi} or \code{psi}
depending on \code{mode}.}

\item{p}{Integer, ARCH order.}

\item{q}{Integer, GARCH order.}

\item{mode}{Character, one of \code{"unitOmega"}, \code{"targetSigma2"}, \code{"freeOmega"}.}

\item{eps}{Numeric buffer in [0,1), enforces \eqn{\tau \le 1-\varepsilon}.}
}
\value{
Named numeric vector \code{c(omega, alpha_1..alpha_p, beta_1..beta_q)}.
}
\description{
Transforms \code{zeta = (eta0, kappa_1..kappa_{p+q}, [xi|psi])} to
constrained \code{phi = (omega, alpha_1..alpha_p, beta_1..beta_q)} while
enforcing \eqn{\alpha_i \ge 0}, \eqn{\beta_j \ge 0}, and
\eqn{\sum \alpha_i + \sum \beta_j < 1}.
}
\examples{
# eta0, kappa1..kappa2 for p+q=2
zeta <- c(0, rep(0, 2))
GARCH_params_to_phi(zeta, p = 1, q = 1)

}
