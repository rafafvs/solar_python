% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sGARCH-internals.R
\name{sGARCH_scenarios}
\alias{sGARCH_scenarios}
\title{Simulate scenarios for an sGARCH(p, q) with 2-component Gaussian mixture shocks}
\usage{
sGARCH_scenarios(
  B = 100,
  n = 1000,
  eps0 = 0,
  sigma2_0,
  omega,
  alpha,
  beta,
  mu,
  sd,
  probs
)
}
\arguments{
\item{B}{Integer, number of scenarios (paths).}

\item{n}{Integer, path length.}

\item{eps0}{optional numeric initial epsilons to prepend (length p+q).}

\item{omega}{Numeric scalar, strictly positive, intercept of the GARCH model.}

\item{alpha}{Numeric scalar, strictly positive, ARCH parameters.}

\item{beta}{numeric length q (>=0).}

\item{mu}{Numeric length-2, component means \code{c(mu1, mu2)}.}

\item{sd}{Numeric length-2, component standard deviations \code{c(sd1, sd2)}.}

\item{p_mix}{Numeric scalar or length-\code{n} vector in (0,1), mixture prob for component 1.}
}
\value{
A list of length \code{B}; each element is a tibble with columns \code{t, j, x, sigma2}.
}
\description{
Generates \code{B} Monte Carlo paths of length \code{n} from an sGARCH process
with innovations \eqn{\varepsilon_t = \sqrt{\sigma_t^2}\,U_t}, where
\eqn{U_t} is a two-component Gaussian mixture with time-varying or constant
mixing probability.
}
\examples{
set.seed(123)
sim <- sGARCH_scenarios(B = 10, n = 500,
omega = 0.05, alpha = 0.05, beta = 0.9,
mu = c(0, 0), sd = c(1, 2), p_mix = 0.2)

}
