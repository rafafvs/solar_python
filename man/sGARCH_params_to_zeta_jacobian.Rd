% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sGARCH-internals.R
\name{sGARCH_params_to_zeta_jacobian}
\alias{sGARCH_params_to_zeta_jacobian}
\title{Jacobian of the (unconstrained -> constrained) GARCH reparametrization}
\usage{
sGARCH_params_to_zeta_jacobian(zeta, p, q, mode = "unitOmega", eps = 0)
}
\arguments{
\item{zeta}{Numeric vector of unconstrained parameters: \code{eta0} (mass),
\code{kappa_1..kappa_{p+q}} (softmax logits), and optionally \code{xi} or \code{psi}
depending on \code{mode}.}

\item{p}{Integer, ARCH order.}

\item{q}{Integer, GARCH order.}

\item{mode}{Character, one of \code{"unitOmega"}, \code{"targetSigma2"}, \code{"freeOmega"}.}

\item{eps}{Numeric buffer in [0,1), enforces \eqn{\tau \le 1-\varepsilon}.}
}
\value{
A numeric matrix of size \code{(1+p+q) x (1+(p+q-1)+extra)},
where \code{extra} is 0, 1 for \code{xi}, or 1 for \code{psi} depending on \code{mode}.
}
\description{
Builds the Jacobian matrix \eqn{\partial \phi / \partial \zeta} for the mapping
\code{zeta -> phi}, assuming the \strong{reduced} softmax parameterization
(last \code{kappa} fixed to 0). Rows are ordered as
\code{(omega, alpha_1..alpha_p, beta_1..beta_q)}.
}
\examples{
zeta <- c(0, 0, 0)  # eta0, kappa1 (since last fixed), and psi/xi if needed
GARCH_params_to_zeta_jacobian(zeta, p = 1, q = 1)

}
